import { Mail, Phone, User, Camera, ShieldCheck } from "lucide-react";
import { useAuth } from "../../context/AuthContext";
import { PageHeader, ConnectedNotice } from "../../components/student/StudentFeatureShell";
export default function Profile(){const {user,profile}=useAuth();const name=profile?.full_name||profile?.name||user?.user_metadata?.full_name||"Student";const email=profile?.email||user?.email||"—";return <div className="space-y-6"><PageHeader eyebrow="Account" title="Profile" description="View the information associated with your student account."/><ConnectedNotice>Profile information below comes from the existing authenticated profile context. Editing is intentionally disabled until a safe profile-update workflow is connected.</ConnectedNotice><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="h-28 bg-gradient-to-r from-blue-600 via-indigo-600 to-violet-600"/><div className="px-5 pb-6 sm:px-7"><div className="-mt-10 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div className="flex items-end gap-4"><div className="flex h-20 w-20 items-center justify-center rounded-2xl border-4 border-white bg-blue-50 text-blue-600 shadow-sm"><User size={32}/></div><div><h2 className="text-xl font-bold text-slate-950">{name}</h2><p className="text-sm text-slate-500">Student account</p></div></div><button disabled className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-400"><Camera size={16}/>Change photo</button></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><div className="rounded-xl border border-slate-200 p-4"><Mail size={18} className="text-blue-600"/><p className="mt-3 text-xs text-slate-400">Email</p><p className="mt-1 text-sm font-semibold text-slate-800 break-all">{email}</p></div><div className="rounded-xl border border-slate-200 p-4"><Phone size={18} className="text-blue-600"/><p className="mt-3 text-xs text-slate-400">Phone</p><p className="mt-1 text-sm font-semibold text-slate-800">{profile?.phone||"Not provided"}</p></div></div><div className="mt-4 flex items-center gap-2 rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700"><ShieldCheck size={17}/>Authentication remains managed by the existing secure auth system.</div></div></section></div>}
