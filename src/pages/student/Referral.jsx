import {useCallback,useEffect,useState} from "react";import {Gift,Plus,Copy} from "lucide-react";import {createReferralCode,getMyReferrals} from "../../services/studentService";import {EmptyState,ErrorState,LoadingState,PageHeader,money} from "../../components/student/StudentDataState";
export default function Referral(){const[data,setData]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(null),[creating,setCreating]=useState(false);const load=useCallback(async()=>{setLoading(true);setError(null);try{setData(await getMyReferrals())}catch(e){console.error(e);setError("We couldn't load your referral information.")}finally{setLoading(false)}},[]);useEffect(()=>{load()},[load]);async function create(){setCreating(true);setError(null);try{await createReferralCode();await load()}catch(e){console.error(e);setError("We couldn't create a referral code.")}finally{setCreating(false)}}return <div className="space-y-6"><PageHeader eyebrow="Achievements" title="Referral" description="Share your referral code and track qualifying referrals." action={<button onClick={create} disabled={creating} type="button" className="inline-flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50"><Plus size={16}/>{creating?"Creating...":"Create code"}</button>}/>{loading?<LoadingState/>:error?<ErrorState message={error} onRetry={load}/>:data.length?<div className="space-y-3">{data.map(x=><div key={x.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs text-slate-500">Referral code</p><p className="mt-1 font-bold tracking-wide">{x.referral_code}</p></div><button onClick={()=>navigator.clipboard?.writeText(x.referral_code)} type="button" className="inline-flex items-center gap-2 text-sm font-semibold text-blue-600"><Copy size={15}/>Copy</button></div><div className="mt-4 grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-500">Status</p><p className="mt-1 font-semibold">{x.status}</p></div><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-500">Reward</p><p className="mt-1 font-semibold">{money(x.reward_amount)}</p></div></div></div>)}</div>:<EmptyState title="No referral codes yet" description="Create a referral code to start tracking referrals." icon={Gift} action={<button onClick={create} type="button" className="inline-flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white"><Plus size={16}/>Create referral code</button>}/>}</div>}
